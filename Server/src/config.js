const DB_HOST = 'localhost';
const DB_PORT = 27017;
const DB_NAME = 'ether';
const JWT_SECRET = 'lanhkhoc';
const SALT_ROUND = 10;
const APP_PORT = 8080;

module.exports = {
  DB_HOST,
  DB_PORT,
  DB_NAME,
  JWT_SECRET,
  SALT_ROUND,
  APP_PORT
};
